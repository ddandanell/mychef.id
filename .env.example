# Server port for local Express dev (Vercel ignores this)
PORT=5000

# Postgres connection string (Neon format) — only required once you switch
# storage from MemStorage to a Drizzle-backed implementation. Until then the
# app runs in memory and quote submissions are lost on restart / cold start.
# DATABASE_URL=postgres://user:password@host/dbname?sslmode=require
