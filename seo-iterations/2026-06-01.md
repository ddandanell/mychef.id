# SEO Iteration — 2026-06-01

## Data snapshot (last 28d)
- Total clicks: 61
- Total impressions: 2,787
- Avg position: 12.31
- Pages with traffic: 14
- CTR_GAP opportunities: 1 page (homepage, 1065 imp, 1.878% CTR, pos 10.37)
- POSITION_PUSH opportunities: 1 query ("private chef bali" on homepage, 98 imp, pos 15.45, 3 clicks)

## Top issues identified

1. **https://mychef.id/** — CTR_GAP + POSITION_PUSH — 1065 impressions, 1.878% CTR, pos 10.37; query "private chef bali" at pos 15.45 with 98 imp — CDN is serving stale title `myCHEF.id — Private Chef & Event Experiences in Bali` despite code having `Private Chef in Bali — In-Villa Dining Since 2012 | myCHEF`. Any deploy triggers CDN purge and fixes. Also: all pre-rendered pages use "Home" as anchor text for the homepage link — changing to "Private Chef in Bali" sends stronger topical signal to Google.

2. **https://mychef.id/private-chef-menteng** — CTR_GAP adjacent — 92 impressions, 1.087% CTR, pos 11.57. postbuild.ts has an Indonesian title (`Sewa Chef ke Rumah di Menteng Jakarta | Private Chef Profesional — myCHEF`) but GSC shows organic queries are 80%+ English ("private chef", "fine dining private chef", "private chef dinner"). Indonesian title underserves English-intent searchers. Fix: switch to English title targeting the actual query traffic.

3. **https://mychef.id/ubud** — 0% CTR at position 9.0 — 13 impressions, 0 clicks. CDN-cached canonical points to `/locations/ubud` (wrong — should be `/ubud`). seoTitle in cityData.ts (`Private Chef Ubud, Bali — Jungle Villa Dining & Wellness Menus`) has not been deployed to CDN yet. Any deploy regenerates the static HTML and fixes both canonical and title.

4. **https://mychef.id/gianyar** — 133 impressions, 0% CTR, pos 14.77 — but most traffic is noisy/irrelevant queries (recipe/cooking show searches). Title already good (`Babi Guling & Ceremonial Menus`). Position 14.77 = page 2 → low CTR is expected. Defer; no fix needed.

## Plan for this iteration

Ship items 1–3. Defer gianyar (page 2 position, already optimised title).

- Item 1 (homepage): Fix internal link anchor text in postbuild.ts noscript/crawler block. "Home" → "Private Chef in Bali" for the homepage link across all pre-rendered pages. Deployment itself purges CDN and serves already-updated title.
- Item 2 (menteng): Change postbuild.ts menteng entry from Indonesian title/description to English, targeting "private chef menteng jakarta" and "hire private chef jakarta" intent.
- Item 3 (ubud): Update cityData.ts ubud seoTitle/seoDescription to be more action-oriented and compelling for "private chef ubud" click intent (pos 9 should earn clicks).

## Changes shipped

All three code changes were committed to `ddandanell/mychef.id` at commit `63204e6` on 2026-06-01.

### Item 1 — Homepage anchor text (`scripts/postbuild.ts`)
Changed the internal nav link rendered into every pre-rendered page's noscript block:
- Before: `<li><a href="/">Home</a></li>`
- After: `<li><a href="/">Private Chef in Bali — myCHEF</a></li>`

Topical anchor text strengthens Google's association of the homepage with "private chef bali" — the site's primary commercial query.

### Item 2 — Menteng title/description (`scripts/postbuild.ts`)
Switched postbuild.ts menteng entry from Indonesian to English:
- Before title: `Sewa Chef ke Rumah di Menteng Jakarta | Private Chef Profesional — myCHEF`
- After title: `Private Chef in Menteng, Jakarta — Fine Dining & Weekly Meal Prep | myCHEF`
- Before desc: Indonesian copy targeting local Indonesian searchers
- After desc: `Hire a vetted private chef in Menteng, Jakarta Pusat. Embassy entertaining, diplomatic residences, fine dining menus, weekly meal prep. Background-checked. WhatsApp booking.`

GSC data shows 80%+ of Menteng queries are English-language ("private chef", "fine dining private chef", "private chef dinner"). Indonesian title was suppressing CTR for actual intent.

### Item 3 — Ubud SEO title/description (`shared/cityData.ts`)
Updated ubud entry to be click-intent-driven at position 9:
- Before title: `Private Chef Ubud, Bali — Jungle Villa Dining & Wellness Menus | myCHEF`
- After title: `Private Chef Ubud, Bali — Hire in 1 Hour for Jungle Villas & Retreats | myCHEF`
- Before desc: `Private chef in Ubud's jungle villas. Organic farm-to-table menus, Balinese ceremony cuisine, wellness retreat catering. Background-checked. WhatsApp.`
- After desc: `Book a vetted private chef for your Ubud villa: organic farm-to-table, Balinese ceremony cooking, wellness retreat menus. Background-checked. WhatsApp booking in 1 hour.`

At position 9 with 0 clicks (13 imp), the old title lacked urgency. Added "Hire in 1 Hour" CTA and "Retreats" to match wellness-retreat intent common in Ubud. Canonical bug (`/locations/ubud` instead of `/ubud`) will also be fixed by the postbuild re-run triggered on deploy.

## Measured baseline (pre-deploy)

Live verification via Vercel CDN fetch at 2026-06-01 ~01:21 UTC:

| URL | Live title (CDN) | Live canonical | CDN age |
|-----|-----------------|----------------|---------|
| `https://mychef.id/` | `myCHEF.id — Private Chef & Event Experiences in Bali` (stale) | `/` OK | 225,397 s (~62 h) |
| `https://mychef.id/ubud` | `Private Chef Ubud \| Jungle Retreats & Wellness — myCHEF` (old) | `/locations/ubud` WRONG | 87,491 s (~24 h) |
| `https://mychef.id/private-chef-menteng` | could not fetch | — | — |

Homepage CDN last-modified: Fri 29 May 2026 10:45 UTC. Ubud CDN last-modified: Sun 31 May 2026 01:05 UTC (from `master3mychef` deploy).

## Deployment status — ACTION REQUIRED

**The `mychef.id` GitHub repo is NOT connected to the Vercel project serving the live domain.**

- Vercel project `master3mychef` (`prj_VkMbGIUciFBk2VE0EUy2SikfWOgK`) deploys from `ddandanell/master3mychef`.
- This session's changes live in `ddandanell/mychef.id` at commit `63204e6`.
- There is no auto-sync GitHub Action between the two repos.
- All recent Vercel deployments today are from `master3mychef` repo (David Dandanell's chef image + prerender fixes).

**To deploy the SEO changes**, cherry-pick or copy the diff from `63204e6` into `ddandanell/master3mychef`:

Files changed:
1. `scripts/postbuild.ts` — menteng entry + anchor text change
2. `shared/cityData.ts` — ubud seoTitle/seoDescription

After any deploy from `master3mychef`, CDN cache will be purged and all three fixes go live simultaneously.

## Next iteration backlog

1. **Homepage position push** — "private chef bali" at pos 15.45 (98 imp, 3 clicks). Once anchor text fix deploys and CDN re-crawls, monitor for movement. If still >12 after one crawl cycle, consider a `/private-chef-bali` landing page with dedicated H1.
2. **Ubud click-through** — re-check GSC 14 days post-deploy. If pos 9 still 0 clicks, investigate SERP feature displacement.
3. **Menteng CTR** — baseline 1.087% with 92 imp. English title should push above 2% within 2-3 crawl cycles.
4. **Gianyar** (deferred) — 133 imp, 0 CTR, pos 14.77. Queries are recipe/cooking-show noise. Not actionable with title/meta.
5. **Homepage CDN title** — already fixed in source. Next deploy from `master3mychef` will purge CDN.

## Notes for human reviewer

- **Repo split is the key blocker.** The SEO automation loop writes to `ddandanell/mychef.id`; the deployed site runs from `ddandanell/master3mychef`. Either connect `mychef.id` to Vercel, or add a GitHub Action to cherry-pick SEO commits into `master3mychef`. Until then, every iteration requires a manual merge step before changes go live.
- The `master3mychef` repo had 10+ deployments today (playwright, prerender, chef image fixes). The SEO diff is small and non-conflicting — safe to cherry-pick immediately.
- No pricing copy was touched in this iteration.
- Gianyar was correctly deferred — noisy query traffic from Bali cooking-show culture cannot be fixed with a title change.
