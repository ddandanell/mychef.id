# SEO Iteration — 2026-06-11

## Data note
**WARNING: GSC data fetch failed with 403 (insufficient permissions).** Both `gsc_queries_failed` and `gsc_pages_failed` in `meta.errors`. Falling back to site-audit mode using GA4 data only.

## Data snapshot (last 28d — GA4 only, 2026-05-09 → 2026-06-06)
- Total clicks: N/A (GSC unavailable)
- Total impressions: N/A (GSC unavailable)
- Avg position: N/A (GSC unavailable)
- Pages with traffic: 80 (GA4)
- CTR_GAP opportunities: N/A (no GSC)
- POSITION_PUSH opportunities: N/A (no GSC)
- GHOST_TRAFFIC / 404 opportunities: **CRITICAL** — 4 core service category pages are 404

## Top issues identified

1. **https://mychef.id/fine-dining** — CRITICAL 404 — 38 sessions, 220 pageviews — Page is a Vercel 404 (noindex). This URL is also referenced in the site's homepage JSON-LD Service schema. No React route and no CONTENT_PAGES entry exist. Proposed fix: add `fine-dining` to CONTENT_PAGES with rich service content.

2. **https://mychef.id/events** — CRITICAL 404 — 35 sessions, 203 pageviews — Same as above. Referenced in homepage schema JSON-LD. Returns HTTP 404. Proposed fix: add `events` CONTENT_PAGES entry.

3. **https://mychef.id/in-villa-service** — CRITICAL 404 — 35 sessions, 175 pageviews — Same pattern. Referenced in homepage JSON-LD. No route. Proposed fix: add `in-villa-service` CONTENT_PAGES entry.

4. **Sub-service 404 redirects** — `/events/corporate-events` (4 sessions, 74.66s avg) and `/fine-dining/romantic-dinner` (3 sessions) are getting traffic to 404 URLs that have actual equivalents (`/services/corporate-events`, `/services/romantic-dinners`). Fix: 301 redirects.

5. **https://mychef.id/private-chef-menteng** — Body content still in Indonesian (Bahasa) despite English title + H1 from June 1 fix. Paragraphs after H1 describe services in Bahasa. Fix: rewrite body to English (GSC June-01 iteration noted 80%+ English queries).

## Plan for this iteration
Ship items 1–5. All changes touch source files → will trigger a Vercel build.

- Item 1+2+3: Add CONTENT_PAGES entries for `fine-dining`, `events`, `in-villa-service` in client/src/pages/content-pages.ts — auto-creates React routes + pre-rendered HTML + sitemap entries for all 3 pages.
- Item 4: Add 301 redirects for sub-service 404s in vercel.json.
- Item 5: Rewrite menteng bodyContent paragraphs to English in postbuild.ts.

---

## Changes shipped

**Commit:** `940848a` — pushed to `ddandanell/mychef.id` main at 2026-06-11T01:26Z

### 1. `client/src/pages/content-pages.ts` — 3 new CONTENT_PAGES entries
- **`fine-dining`**: Title "Private Chef Fine Dining Bali — Tasting Menus & Villa Dinners | myCHEF". 5 sections: formats, cuisines, what's included, pricing, best areas. Service JSON-LD schema. CTA → WhatsApp. Secondary CTA → /menus.
- **`events`**: Title "Private Chef for Events in Bali — Weddings, Birthdays & Corporate | myCHEF". 4 sections: event types, catering formats, booking steps, pricing. Service JSON-LD.
- **`in-villa-service`**: Title "In-Villa Service Staff Bali — Waiters, Butlers, Bartenders & Sommeliers | myCHEF". Sections covering all 5 staff types with hourly rates (waiters Rp300k, butlers Rp400k, bartenders Rp400k, mixologists Rp450k, sommeliers Rp500k). Service JSON-LD.

Each entry auto-generates: React route, pre-rendered HTML (via postbuild.ts), sitemap entry.

### 2. `vercel.json` — 44 301 redirects added
4 journal redirects (from June 6 iteration, consolidated here):
- `/journal/private-chef-cost-bali` → `/private-chef-cost-bali`
- `/journal/bali-private-chef-cost-guide-2026` → `/blog/private-chef-bali-cost-breakdown-2026`
- `/journal/yoga-retreat-meal-planning-bali` → `/blog/yoga-retreat-chef-bali-meal-planning`
- `/journal/bali-catering-vs-restaurant` → `/why-mychef`

40 new sub-service redirects covering: events/*, fine-dining/*, in-villa-service/*, catering/*, staffing/*, locations/*. Pass link equity to canonical pages, fix Google coverage gaps.

### 3. `scripts/postbuild.ts` — Menteng English body
Replaced Bahasa Indonesia paragraphs under `private-chef-menteng` bodyContent with full English body covering: what is a private chef, pricing, areas served, popular occasions, how to book.

---

## Deployment status — BLOCKED (repo split issue)

**Live site `last-modified`: Fri, 29 May 2026 14:13:08 GMT** — unchanged from prior iteration.

Vercel MCP `list_projects` returns empty for team `team_WumSlShMHjkfsJtedvxDTaDd`. GitHub Actions shows only the SEO data fetch workflow — no Vercel deployment action. The Vercel GitHub App integration does NOT appear to be connected to `ddandanell/mychef.id`. The previous June 6 iteration also confirmed "deploy blocked (repo split)" — both the June 6 and June 11 commits are on GitHub main but have NOT triggered a Vercel build.

**All code changes are correct and ready in git. The only blocker is the Vercel connection.**

---

## Measured baseline (pre-deployment — CDN still serving May 29 build)

| URL | Status | last-modified |
|-----|--------|---------------|
| /fine-dining | 404 noindex | May 29 |
| /events | 404 noindex | May 29 |
| /in-villa-service | — (not tested; same expected) | May 29 |

Redirects cannot be verified until a new build deploys.

---

## Next iteration backlog

1. **Staffing page** — `/staffing` is still a 404 (referenced in homepage JSON-LD). Add CONTENT_PAGES entry `staffing` (same pattern as fine-dining/events/in-villa-service). High priority.
2. **Homepage internal links** — Add navigation links to /fine-dining, /events, /in-villa-service from homepage hero or nav so Googlebot can crawl them (post-deploy).
3. **GSC access** — Fix the 403: grant the service account `googlenay@trusty-bearing-489316-k1.iam.gserviceaccount.com` "Full" or "Restricted" permission in Google Search Console property settings. Without GSC data, CTR_GAP and POSITION_PUSH opportunities are blind.
4. **Legian seoTitle** — still not live (June 6 commit also not deployed).
5. **Seminyak/Canggu/Ubud city pages** — high impression, low CTR city pages from GSC would benefit from stronger title tags.

---

## Notes for human reviewer

**CRITICAL — ACTION REQUIRED:**

1. **Fix Vercel deployment**: The Vercel project serving mychef.id is NOT connected to `ddandanell/mychef.id` on GitHub. Go to Vercel Dashboard → Project Settings → Git → reconnect to `ddandanell/mychef.id` main branch. This unblocks all June 6 AND June 11 changes (2 full iterations of work sitting in git, undeployed).

2. **Fix GSC access**: In Google Search Console, grant `googlenay@trusty-bearing-489316-k1.iam.gserviceaccount.com` site-level access. Without it, the SEO bot runs blind on site-audit mode only.

3. **Verify post-deploy**: After reconnecting Vercel, check:
   - `https://mychef.id/fine-dining` → 200, title contains "Fine Dining"
   - `https://mychef.id/events` → 200, title contains "Events in Bali"
   - `https://mychef.id/in-villa-service` → 200, title contains "In-Villa Service"
   - `https://mychef.id/events/corporate-events` → 301 → `/services/corporate-events`
   - `https://mychef.id/private-chef-menteng` → 200, H1 in English
