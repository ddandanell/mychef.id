# SEO Iteration — 2026-06-16

## Data note
**WARNING: GSC data fetch failed with 403 (insufficient permissions).** Both `gsc_queries_failed` and `gsc_pages_failed`. Falling back to site-audit mode using GA4 data only (same as June 11 iteration — GSC 403 is now 3 consecutive iterations unresolved).

## Data snapshot (last 28d — GA4 only, 2026-05-14 → 2026-06-11)
- Pages with traffic: 80 (GA4)
- CTR_GAP / POSITION_PUSH opportunities: N/A (no GSC data)
- GHOST_TRAFFIC critical (confirmed 404s receiving real sessions + conversions):
  - `/staffing`: 12 sessions, **8 conversions = 67% CVR** — HIGHEST CONVERTING PAGE ON SITE
  - `/locations`: 15 sessions, **8 conversions = 53.3% CVR**
  - `/fine-dining`, `/events`, `/in-villa-service`: fixed June 11 — not yet live on production (deploy blocker)

## Top issues identified

1. **https://mychef.id/staffing** — CRITICAL 404 — 12 sessions, 8 conversions, 67% CVR. Referenced in homepage JSON-LD `Service` schema. No CONTENT_PAGES entry exists — pre-render has no rich content, no structured data. Highest-ROI fix available: adding a rich CONTENT_PAGE with Service + FAQPage schema.

2. **https://mychef.id/locations** — CRITICAL 404 — 15 sessions, 8 conversions, 53.3% CVR. Sub-pages (`/locations/seminyak` etc.) redirect to city pages via vercel.json, but the hub `/locations` itself has no pre-rendered content or React route. Fix: add CONTENT_PAGES entry.

3. **https://mychef.id/catering** — 42 sessions, 2 conversions (4.8% CVR). Page exists with rich content but no FAQPage schema. Adding FAQ structured data targets rich snippet eligibility for "how much does catering cost in Bali?" and similar queries.

## Plan for this iteration
Ship items 1–3. All changes touch `client/src/pages/content-pages.ts` only → triggers Vercel build.

- Item 1: Add `staffing` CONTENT_PAGE with Service + FAQPage JSON-LD schema
- Item 2: Add `locations` CONTENT_PAGE with hub content linking to all city pages
- Item 3: Add FAQPage schema to existing `catering` structuredData array

---

## Changes shipped

**Commit:** `455a76e` — pushed to `ddandanell/mychef.id` main at 2026-06-16T01:23Z

### 1. `staffing` CONTENT_PAGE added
Title: "Private Chef & Villa Staff Placement Bali — Live-In, Household & Permanent | myCHEF"
- 5 sections: placement types, who uses placements, the process (4 steps), pricing, why use myCHEF
- Service JSON-LD + FAQPage schema (4 questions: live-in chef cost, live-in vs placement, placement timeline, monthly chef hire)
- CTA → WhatsApp preset for placement enquiry; secondary CTA → /villa-partners

### 2. `locations` CONTENT_PAGE added
Title: "Private Chef Locations in Bali — All Areas We Cover | myCHEF Indonesia"
- 4 sections: main locations served (9 bullet areas), sub-area + neighborhood pages (links to existing sub-area pages), travel policy + surcharges, Jakarta locations
- Service JSON-LD with `areaServed` array covering 6 main cities
- CTA → WhatsApp; slug: `locations`

### 3. FAQPage schema added to `catering`
Added `FAQPage` structured data block to the existing `catering` entry's `structuredData` array. 4 questions targeting rich snippet queries:
- "How much does catering cost in Bali?" → Rp 800k/hr answer
- "What is the minimum number of guests for catering in Bali?" → 8 guests
- "Is myCHEF a catering company?" → private chef alternative to catering explanation
- "How much notice do I need to book catering in Bali?" → 2 days minimum

---

## Deployment status — BLOCKED (4th consecutive iteration)

**Live site `last-modified`: Fri, 29 May 2026 14:13:08 GMT** — unchanged since May 29.

Production `mychef.id` is still served from a DIFFERENT Vercel project / codebase. The `mychef-id` project (`prj_p3nHnVp5mmJQ7bAYQROJX9ievqiY`) builds correctly and preview URL works: `mychef-r055z7dtg-daviddandanell-9392s-projects.vercel.app`. But `mychef.id` custom domain is NOT attached to this project.

June 6 changes (Legian title), June 11 changes (fine-dining, events, in-villa-service pages + 40 redirects + menteng English body), and June 16 changes (staffing, locations, catering FAQ) are ALL accumulating in git main — deployed to preview, NOT to production.

---

## Measured baseline (pre-deployment)

| URL | Production status | Preview status |
|-----|------------------|----------------|
| /staffing | 404 | **200 ✅** (verified 2026-06-16T01:24Z — title + FAQPage schema confirmed) |
| /locations | 404 | **200 ✅** (verified 2026-06-16T01:24Z — title + Service schema confirmed) |
| /fine-dining | 404 | 200 (confirmed June 11 preview) |
| /events | 404 | 200 (confirmed June 11 preview) |
| /in-villa-service | 404 | 200 (confirmed June 11 preview) |

---

## Next iteration backlog

1. **Fix Vercel connection (CRITICAL, human action)** — 4 iterations of work pending. See notes below.
2. **Fix GSC 403 (CRITICAL, human action)** — 3 iterations blind on CTR_GAP / POSITION_PUSH.
3. **Internal linking** — Add links from homepage to /staffing, /locations, /fine-dining (post-deploy).
4. **Seminyak/Canggu/Ubud title tags** — High impression, low CTR city pages (need GSC to quantify).
5. **Catering page H1** — Currently "Event Catering & Party Dinner Service — Bali | myCHEF Indonesia" — could be more search-intent aligned.

---

## Notes for human reviewer

**CRITICAL — ACTION REQUIRED (same note for 4th iteration in a row):**

1. **Fix Vercel deployment** — The Vercel project serving `mychef.id` is NOT `ddandanell/mychef.id`. Go to Vercel Dashboard → Project Settings → Git → reconnect to `ddandanell/mychef.id` main branch. OR connect the `mychef.id` custom domain to the existing `mychef-id` project (`prj_p3nHnVp5mmJQ7bAYQROJX9ievqiY`). 4 full iterations of SEO work (June 1, June 6, June 11, June 16) are undeployed to production. Every day of delay is traffic going to 404 pages with known conversion intent — `/staffing` alone is generating 8 conversions at 67% CVR with nothing to land on.

2. **Fix GSC access** — Grant `googlenay@trusty-bearing-489316-k1.iam.gserviceaccount.com` site-level access in Google Search Console for `sc-domain:mychef.id`. Without it, the SEO bot cannot see impressions, CTR, or position data — running blind on GA4 only.

3. **Post-deploy verification checklist:**
   - `https://mychef.id/staffing` → 200, title contains "Chef & Villa Staff Placement"
   - `https://mychef.id/locations` → 200, title contains "Locations in Bali"
   - `https://mychef.id/fine-dining` → 200, title contains "Fine Dining"
   - `https://mychef.id/events` → 200, title contains "Events in Bali"
   - `https://mychef.id/in-villa-service` → 200, title contains "In-Villa Service"
   - `https://mychef.id/events/corporate-events` → 301 → `/services/corporate-events`
   - `https://mychef.id/private-chef-menteng` → 200, body in English
