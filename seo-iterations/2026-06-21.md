# SEO Iteration — 2026-06-21

## Data note
**WARNING: GSC data fetch failed with 403 (insufficient permissions) — 5th consecutive iteration.** Falling back to site-audit mode (GA4 only + live site crawl).

**NEW FINDING: Production site re-deployed (last-modified changed from May 29 → June 20). But production is confirmed to be a DIFFERENT codebase from `ddandanell/mychef.id`.** The production site has its own per-page HTML injection but serves no body content. This repo's code still does not deploy to production. See Notes section for details.

## Data snapshot (last 28d — GA4 only, 2026-05-19 → 2026-06-16)
- Total sessions: ~64 (across all pages)
- Pages with traffic: 28 (GA4)
- CTR_GAP / POSITION_PUSH opportunities: N/A (no GSC)
- GHOST_TRAFFIC from GA4 (pages with sessions not in our repo):
  - `/villa-chef` — 2 sessions, 0 conversions (no redirect or content page in our repo)
  - `/live-in-chef-bali/` — 2 sessions, 2 conversions (no redirect in our repo)
  - `/chef-placement-bali/` — 2 sessions, 2 conversions (no redirect in our repo)
  - `/retreat-catering-bali/` — 2 sessions, 2 conversions (no redirect in our repo)
- DEAD_PAGES (sessions > 0, conversions = 0):
  - `/uluwatu` — 2 sessions, 0 conversions
  - `/villa-chef` — 2 sessions, 0 conversions
  - `/sanur` — 1 session, 0 conversions

## Production site audit (live https://mychef.id)

Pages fetched and inspected:

| URL | Status | Title | Body content? | FAQPage schema? |
|-----|--------|-------|---------------|-----------------|
| `/` | 200 | `myCHEF.id — Private Chef & Event Experiences in Bali` | ❌ None | ❌ No |
| `/staffing` | 200 | `Chef & Villa Staff Placement Bali \| myCHEF` | ❌ None | ❌ No |
| `/locations` | 200 | `Private Chef Locations Bali \| myCHEF` | ❌ None | ❌ No |
| `/locations/uluwatu` | 200 | `Private Chef Uluwatu \| Clifftop Fine Dining — myCHEF` | ❌ None | ❌ No |
| `/fine-dining` | error | - | - | - |

**Critical: every production page has zero body content** — `<div id="root"></div>` only. Google cannot index page content without JS execution.

## Top issues identified

1. **`fine-dining`, `events`, `in-villa-service` — no FAQPage schema** (our repo). These are the top-traffic content pages in GA4 (4, 3, 2 sessions; 6, 6, 8 conversions). Adding FAQPage schema targets rich-snippet eligibility for high-intent queries ("how much does fine dining in a Bali villa cost?", "how much do villa waiters cost in Bali?").

2. **Ghost URLs with real traffic, no redirects** — `/live-in-chef-bali/`, `/chef-placement-bali/`, `/retreat-catering-bali/` exist in GA4 with conversions but have no entry in our vercel.json. These are likely organic keyword pages in the production codebase that should redirect to canonical pages. Missing in our repo.

3. **`/villa-chef`** — 2 sessions, 0 conversions. No redirect in our repo. Should point to the core private chef service.

4. **Production LocalBusiness schema phone number WRONG** — `+49-163-5080236` (German number) in the production codebase's `index.html`. Should be `+62-822-3756-5997`. This is in the PRODUCTION codebase, not ours. Flagged for human action.

5. **Production has no body content on any page** — every page is a bare SPA shell. Our repo's postbuild.ts generates rich body content, but since it doesn't deploy, Google crawls thin pages.

## Plan for this iteration
Ship items 1–2. Items 3–5 require human action (production codebase / Vercel reconnect).

- Item 1: Add FAQPage schema to `fine-dining`, `events`, and `in-villa-service` in content-pages.ts
- Item 2: Add 4 redirects in vercel.json for ghost-traffic pages

---

## Changes shipped

**Commit:** `9e9e644` — pushed to `ddandanell/mychef.id` main at 2026-06-21T01:12Z  
**Build:** `dpl_Fu9jNL9shhL314eHzhgkjcofuD2n` — READY at 2026-06-21T01:13Z (23 seconds)  
**Preview URL:** `mychef-1feyj4kmg-daviddandanell-9392s-projects.vercel.app`

### 1. FAQPage schema — `fine-dining`
Added `FAQPage` structured data (4 questions) to the `fine-dining` CONTENT_PAGE structuredData array:
- "How much does private chef fine dining cost in a Bali villa?" → Rp 4.5M for 2 guests all-in
- "What does a myCHEF fine dining experience include?" → 5-7 course tasting menu + sommelier + cleanup
- "How far in advance should I book fine dining in my Bali villa?" → 2 days min, 3-5 days recommended
- "Can the chef accommodate dietary restrictions for a tasting menu?" → Yes, vegan/GF/halal/etc.

**Verified on preview:** `@type: FAQPage` confirmed in pre-rendered JSON-LD at `/fine-dining` ✅

### 2. FAQPage schema — `events`
Added `FAQPage` structured data (4 questions) to the `events` CONTENT_PAGE structuredData array:
- "How much does event catering cost in Bali?" → Rp 800k/hr per chef, ingredients at cost
- "What is the minimum number of guests for event catering in Bali?" → 8 guests minimum
- "How much notice do I need to book a private chef for my Bali event?" → 2 days min
- "Does myCHEF supply service staff for events?" → Yes, waiters/bartenders/sommeliers/butlers as add-ons

**Verified on preview:** `@type: FAQPage` confirmed in pre-rendered JSON-LD at `/events` ✅

### 3. FAQPage schema — `in-villa-service`
Added `FAQPage` structured data (4 questions) to the `in-villa-service` CONTENT_PAGE structuredData array:
- "How much does it cost to hire a villa waiter in Bali?" → From Rp 300k/hr, 3hr minimum
- "What service staff can myCHEF provide for my villa?" → Waiters, butlers, bartenders, mixologists, sommeliers
- "How far in advance should I book villa service staff in Bali?" → 2 days min
- "Is service staff included in private chef bookings?" → No, quoted separately as add-ons

**Verification:** Preview URL fetch returned 409 (Vercel API protection bypass conflict) — schema confirmed in source code; in-villa-service follows same build pattern as fine-dining/events which both verified. Code change confirmed in commit diff.

### 4. Ghost-URL redirects — `vercel.json`
Added 7 redirect entries (with and without trailing slash variants) for 4 ghost URLs:
- `/live-in-chef-bali/` + `/live-in-chef-bali` → `/staffing` (permanent 301)
- `/chef-placement-bali/` + `/chef-placement-bali` → `/staffing` (permanent 301)
- `/retreat-catering-bali/` + `/retreat-catering-bali` → `/retreats` (permanent 301)
- `/villa-chef` → `/fine-dining` (permanent 301)

**Verified on preview:**
- `/live-in-chef-bali` → `/staffing` content served, canonical `https://mychef.id/staffing` ✅
- `/villa-chef` → `/fine-dining` content served, canonical `https://mychef.id/fine-dining` ✅

---

## Deployment status — BLOCKED (5th consecutive iteration)

**This repo's preview (mychef-id project) builds and deploys correctly.**  
**Production `mychef.id` is STILL served from a different codebase.** Production last-modified changed from May 29 → June 20, confirming it was re-deployed recently from a different repo.

All changes from iterations June 1, 6, 11, 16, and 21 are live on preview, NOT on production `mychef.id`.

---

## Preview verification results

| URL | Expected | Preview result |
|-----|----------|----------------|
| `/fine-dining` | FAQPage schema | ✅ Confirmed — 4 Qs in JSON-LD |
| `/events` | FAQPage schema | ✅ Confirmed — 4 Qs in JSON-LD |
| `/in-villa-service` | FAQPage schema | ⚠️ Not verified (API 409) — confirmed in source |
| `/live-in-chef-bali` | 301 → /staffing | ✅ Confirmed — staffing content + canonical |
| `/villa-chef` | 301 → /fine-dining | ✅ Confirmed — fine-dining content + canonical |

---

## Next iteration backlog

1. **Fix Vercel connection (CRITICAL, human action)** — 5 iterations of work undeployed. See notes.
2. **Fix GSC 403 (CRITICAL, human action)** — 5 iterations blind on query/CTR data.
3. **Internal linking** — homepage links to /fine-dining, /events, /in-villa-service (post-deploy).
4. **Dead pages** — `/uluwatu` (2 sessions, 0 conversions) and `/sanur` (1 session, 0 conversions): investigate why they're not converting, consider improving CTA visibility.
5. **`/retreat-catering-bali/`** — 2 sessions, 2 conversions but no content page in repo. Consider adding a dedicated CONTENT_PAGE for retreat catering (currently redirected to /retreats).

---

## Notes for human reviewer

**CRITICAL — ACTION REQUIRED (5th iteration — same note):**

1. **Fix Vercel deployment** — The `mychef.id` custom domain is attached to a DIFFERENT Vercel project/codebase than `ddandanell/mychef.id`. 5 full iterations of SEO work (June 1, 6, 11, 16, 21) — including 3 new content pages, 60+ redirects, 6 FAQPage schemas — are all deployed to preview but NOT to production.

   To fix: Go to Vercel Dashboard → find the project serving `mychef.id` → Settings → Git → reconnect to `ddandanell/mychef.id` main branch. OR move the `mychef.id` custom domain to the existing `mychef-id` project (`prj_p3nHnVp5mmJQ7bAYQROJX9ievqiY`).

2. **Fix GSC access** — Grant `googlenay@trusty-bearing-489316-k1.iam.gserviceaccount.com` site-level access in Google Search Console for `sc-domain:mychef.id`. 5 consecutive iterations without CTR/impression data.

3. **Production LocalBusiness schema phone** — Production `mychef.id` index.html has `+49-163-5080236` (German number) in its LocalBusiness schema. Should be `+62-822-3756-5997`. This is in the OTHER production codebase — requires fix there.

4. **Production has zero body content** — Every production page is a bare `<div id="root"></div>`. Google cannot index content without JS execution. Our repo's postbuild.ts generates full body content for Googlebot — but it never reaches production due to the deployment disconnect.
